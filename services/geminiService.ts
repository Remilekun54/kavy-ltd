
export const getAIResponse = async (userPrompt: string) => {
  // Service temporarily disabled as per user request (no API key provided)
  // This also prevents "ReferenceError: process is not defined" in production environments.
  return "Our strategic intelligence systems are currently in stealth mode. Please contact KAVY Headquarters at +234 8144130329 for direct executive consultation.";
};